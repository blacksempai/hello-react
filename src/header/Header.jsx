import './Header.css';
import logo from './logo.png'

const Header = () => {
    return (
        <header>
            <img className='App-logo' src={logo} alt="M" />    
        </header>
    );
}

export default Header;